# subdenum

It's a subdomain enumeration tool which take input a domain and run through several threads and use different tools API to extract subdomains which can be further used in bug bounty. 

I have changed it.